<?xml version="1.0" encoding="utf-8"?>

<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.luke.android.travelogy">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.luke.android.travelogy.FlagListActivity"
            android:label="@string/title_flag_list"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".FlagTaskService"
            android:exported="true"
            android:permission="com.google.android.gms.permission.BIND_NETWORK_TASK_SERVICE">
            <intent-filter>
                <action android:name="com.google.android.gms.gcm.ACTION_TASK_READY" />
            </intent-filter>
        </service>
        <service android:name=".FlagIntentService" />


        <activity
            android:name="com.luke.android.travelogy.details.PhotoListActivity"
            android:label="@string/title_flag_detail"
            android:parentActivityName="com.luke.android.travelogy.FlagListActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.luke.android.travelogy.FlagListActivity" />
        </activity>

        <provider
            android:name=".data.FlagProvider"
            android:authorities="com.luke.android.travelogy" />
    </application>
</manifest>